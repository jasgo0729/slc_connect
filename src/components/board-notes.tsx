/**
 * 씨앗판 하단 안내.
 *
 * 목록을 다 훑고 나서 "그런데 이건 뭐지" 싶은 것들만 모았다.
 * 위에 두면 커넥트를 보기 전에 읽어야 할 글이 생기고, 정작
 * 궁금해지는 시점은 다 본 뒤라 아래가 맞다.
 *
 * 분량을 늘리지 않는다. 여기 없는 설명이 필요하면 그건 화면이
 * 스스로 말해야 하는 것이다.
 */
const NOTES = [
  "'사전 개설 커넥트'는 개설자가 없어 인원이 0명부터 시작합니다.",
  '무엇을 어떻게 할지는 첫 만남에서 팀원들과 자율적으로 정해요.',
  '같은 주제의 커넥트가 인사캠과 자과캠에 각각 개설되어 있습니다. 활동 캠퍼스를 확인하고 지원해 주세요.',
  '최소 인원 4명을 채우지 못하면 팀이 결성되지 않을 수 있습니다. 함께할 사람이 있다면 초대 링크를 공유해 주세요.',
];

export function BoardNotes() {
  return (
    <aside className="notes" aria-label="안내">
      <ul>
        {NOTES.map((n) => (
          <li key={n}>{n}</li>
        ))}
      </ul>
    </aside>
  );
}

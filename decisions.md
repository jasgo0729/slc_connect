# 결정 필요 사항

Cross-SLC Connect · 웹사이트 파이프라인 · 8월 3일 회의용

설계 과정에서 답이 필요해진 항목을 모았다. **A는 이번 회의에서 결론이 나야 하고**, B는 설계 중 임시로 정한 것을 승인받는 항목, C는 착수는 막지 않지만 곧 필요한 것이다.

---

## A. 착수를 막고 있는 것

결론이 없으면 해당 부분을 두 번 만들게 된다.

### A-1. 검색을 추천에 흡수할지 (U-03)

| | |
| --- | --- |
| 선택지 | ① 입력창 하나에 응답 형태만 분기 ② 검색과 추천을 별도 화면으로 |
| 영향 | 화면 1개, `/recommend` 대 `/recommend` + `/search` |
| 상태 | 기능 요구사항 문서가 이미 "B-11을 확정으로 둔 상태라 이 항목과 어긋난다"고 지적 |

①이면 `"독서 커넥트"` 같은 지정 탐색에는 목록을, `"요즘 사람을 안 만나서"` 같은 막연한 입력에는 후보 3개를 낸다. 화면이 하나 줄고 초대 링크를 잃은 사람의 경로도 함께 해결된다.

**PL 의견: ①.** 다만 구태희의 UI 설계에 직접 영향이 가므로 함께 확인 필요.

### A-2. 태그 필터 존치 여부 (U-26)

| | |
| --- | --- |
| 문제 | 개설 폼에서 주제 태그가 빠지면서 B-05 태그 필터가 대조할 값을 잃었다 |
| 선택지 | ① 필터에서 태그를 뺀다 ② 개설 폼에 태그를 되살린다 |
| 영향 | `/connects` 쿼리 파라미터, 개설 폼 항목 수 |

추천은 설명문 원문을 읽는 구조로 바뀌어 태그가 없어도 성립하지만 **필터는 그렇지 않다.** ②를 택하면 개설 진입장벽이 조금 올라간다.

스키마에는 태그 테이블을 이미 만들어 두었으므로, 어느 쪽으로 결론이 나도 DB 변경은 없다.

### A-3. 인증 마감 시각 (G-18)

| | |
| --- | --- |
| 문제 | PO 문서 1.1.3.1의 세 시점이 겹친다 |
| 현재 | 토요일 활동 인정 → 활동 다음날 정오 인증 → 일요일 자정 집계 |
| 결과 | 토요일 활동은 일요일 정오 도착, 자정까지 검수. **주 60~90건에 12시간.** |
| 선택지 | ① 인증 마감을 활동 당일 자정으로 당긴다 ② 집계를 월요일 정오로 미룬다 |

3차 회의에서 다루려다 다른 조항을 잘못 짚어 넘어간 항목이다.

**점수 계산 모듈이 이 값을 기다린다.** 주차 귀속 판정 로직의 기준이므로 나중에 바꾸면 이미 저장된 점수를 전부 재계산해야 한다.

### A-4. 도메인 확정 (신규 · 기술)

구글 로그인 클라이언트 등록과 HTTPS 인증서 발급에 **실제 도메인이 필요하다.** 도메인이 없으면 1주차 인프라 작업 전체가 시작되지 않는다.

발송용 도메인도 같은 것을 쓰면 되므로 하나만 정하면 된다.

### A-5. 학교 워크스페이스 외부 앱 로그인 허용 여부 (신규 · 사실 확인)

`g.skku.edu` 계정으로 외부 서비스에 구글 로그인이 되는지 확인이 필요하다. 워크스페이스 관리자가 서드파티 앱 접근을 차단해 둔 경우가 있고, **막혀 있으면 인증 설계 전체를 카카오로 되돌려야 한다.**

개발로 우회할 수 없고 학교에 요청해야 하는 사안이라 리드타임이 길다. 본인 계정으로 아무 서비스에나 구글 로그인을 눌러 보면 5분 안에 확인된다.

---

## B. 설계 중 정한 것 — 승인 필요

기능 요구사항 문서에 없거나 다르게 적혀 있는 것을 설계 과정에서 정했다. 회의에서 확인만 받으면 된다.

### B-1. 팀장은 이탈할 수 없다

G-15는 "전원 동의 하에 이탈 가능, 4명 미만 불가"만 정하고 있고 팀장에 대한 언급이 없다. **팀장 이탈을 허용하면 후임 지정 절차와 화면이 필요해지므로 금지하기로 했다.**

- 팀장 없는 커넥트가 구조적으로 생기지 않는다
- 후임 지정 화면과 교체 트랜잭션이 불필요해진다
- 부득이한 경우는 관리자가 DB에서 직접 처리 (화면 없음)

**확인 필요:** 실제로 팀장이 나가야 하는 상황이 생겼을 때 운영이 감당 가능한지.

### B-2. 개설자와 팀장은 같은 사람, 팀장은 관리자가 교체 가능

- `connects.created_by` = 최초 개설자 (기록용, 불변)
- `memberships.role = 'leader'` = 현재 팀장 (교체 가능)
- **권한 판정은 항상 팀장 쪽을 본다**
- 팀장 지정 기능은 관리자 화면(J-02)에 둔다

B-1과 합치면 팀장 교체는 관리자만 할 수 있다.

### B-3. 정원에 팀장이 포함된다

정원 6명 = 팀장 포함 6명. G-15의 최소 4명도 팀장 포함 4명.

따라서 **개설 직후 커넥트의 인원은 0이 아니라 1이다.** 씨앗판 카드의 인원 시각화가 0/6이 아니라 1/6에서 시작한다.

### B-4. 마감임박 정렬의 기준은 남은 자리 수 (B-06)

모집 마감일이 전 커넥트 공통이므로 남은 시간으로는 정렬이 성립하지 않는다. `정원 − 현재 인원`을 오름차순으로 정렬해 **자리가 얼마 안 남은 커넥트를 위로 올린다.**

정원이 찬 커넥트는 이미 마감 상태로 갈라지므로 실질 최솟값은 "1자리 남음"이다.

### B-5. MBTI는 로그인 없이 응시 가능

홍보용이므로 진입장벽을 두지 않는다. 결과 화면에서 씨앗판이나 추천으로 이동할 때 로그인을 유도하고, 로그인 후 원래 가려던 곳으로 돌아오면서 응시 결과를 계정에 연결한다.

F-05 완료자 집계는 로그인 여부와 무관하게 센다.

### B-6. 자동 실행(크론)을 두지 않는다

모집 마감일이 시즌 공통 날짜이고 운영 조작은 사람이 판단해 실행하므로, 정해진 시각에 자동으로 도는 배치가 필요 없다.

대신 **관리자 대시보드가 "오늘 처리할 것"을 조회 시점에 계산해서 보여준다.** 검수 대기 건수, 정성 확인 대기, 유연 증원 요청, 인원 미달 커넥트, 신청 0명으로 5일 지난 커넥트. D-14가 이미 이 구조로 정의되어 있다.

크론은 조용히 실패해도 아무도 모른다는 게 가장 큰 약점이다.

### B-7. D-05 마감과 D-16 팀 확정은 같은 조작

마감일이 되면 관리자가 한 번 눌러 전원의 상태를 바꾸고 알림을 일괄 발송한다. 마감과 확정 사이의 중간 상태를 두지 않는다.

미달 팀 처리와 인원 흡수(D-14)는 **확정 버튼을 누르기 전** 수동 구간에서 끝낸다.

되돌릴 수 없는 조작이므로 미리보기(확정될 커넥트 수, 미달 수, 발송 대상 인원)를 먼저 보여주고 실행한다.

### B-8. 인증 방식은 구글 로그인 + 학번 1회 결속

- 학교 구글 계정(`g.skku.edu`)만 허용 → **재학생 여부가 실제로 검증된다**
- 최초 1회만 학번을 입력받아 명단 대조 후 결속. 이후에는 묻지 않는다
- 구글 계정과 학번이 1:1로 잠긴다 → 1인 1계정 확보
- **알림 수신 주소가 로그인 계정이 되므로 A-03 이메일 입력 항목이 사라진다**

기능 요구사항 문서의 "학번만으로 들어오므로 본인 확인 강도가 낮다"는 타협이 해소된다. F-10 게임 도전 제한, E-09 추천 호출 제한, K-04 투표 계정당 1회가 전부 이 전제 위에 선다.

**발송 계정은 분리한다.** 학교 계정으로 대량 발송하면 스팸 처리되고 계정 제한 시 로그인까지 함께 죽는다.

### B-9. 인프라 구성

- AWS EC2 한 대(서울 리전)에 앱·DB·리버스 프록시를 Docker Compose로
- **인증 사진은 S3에 저장한다.** DB나 서버 디스크에 두면 백업이 무거워지고 서버 교체 시 유실된다
- HTTPS는 Caddy가 자동 발급·갱신 (웹 푸시의 전제 조건)
- 점수 데이터 백업(일 1회 S3)은 1주차에 반드시 넣는다

---

## C. 곧 정해야 하지만 착수는 막지 않는 것

| 번호 | 항목 | PL 의견 |
| --- | --- | --- |
| U-02 | 지원 취소 허용 여부 | 없으면 겁먹고 신청을 안 할 수 있다. 스키마에는 상태값을 준비해 둠 |
| U-04 | 정원 기본값 | 최소 4명이지만 기본값은 6명 |
| U-06 | 찜 수 노출 범위 | 정확한 숫자는 개설자만 |
| U-07 | 관심도 알림 발송 조건 | 찜 1·5·10개 시점. 신청은 건마다 |
| U-08 | 로그인 유지 기간 | 최대한 오래 |
| U-14 | 추천 예시 키워드 칩 목록 | 생각을 유도하는 방식과 바로 진입시키는 방식 중 택일 |
| U-01 | 간이 웹 게임 종류 | |
| U-13 | 커뮤니티 영역 채택 여부 | 오픈채팅방으로 대체 가능하므로 잘라낼 수 있다 |
| — | G-12 '실시간'의 범위 | 새로고침하면 최신인 정도로 충분해 보인다 |
| — | 확정 대기 구간의 씨앗판 안내 | 마감 후 확정까지 며칠간 참여자가 볼 화면 |

---

## D. 후순위

| 번호 | 항목 |
| --- | --- |
| U-09 | 방학 온라인 만남의 점수와 배율 |
| U-11 | 상금 배분 총액 (방향은 3차 회의에서 결정, 총액만 남음) |
| U-12 | 정성 트랙 평가 주체 |
| U-10 | iOS 웹 푸시 실동작 — 8월 중 스테이징에서 확인 |
| — | K-03 카드뉴스 · K-04 투표 (필요 시점 2월) |

---

## E. 문서 간 불일치 — 정정 필요

결정 사항이 아니라 표현을 맞춰야 하는 것들이다.

**PO 문서 1.2.5.2** — 크로스 커넥트 "한 주에 최대 20점"으로 적혀 있으나, 20점은 주간 한도가 아니라 **1회당 인원수 상한**이다. 주간 상한은 두지 않기로 확정되었다.

**PO 문서 1.4.1.1.5** — 참여자 프로필로 소속 커넥트를 자동 파악해 점수를 자동 배분하는 구조를 요구하나, G-05 관리자 검수를 거치는 구조와 어긋난다. 검수 시 상대 커넥트와 인원만 확인하면 배분은 계산식으로 처리된다.

**기능 요구사항 B-11 / U-03** — B-11이 확정인데 U-03이 미확정이라 서로 어긋난다. A-1에서 함께 정리된다.

---

## 회의 진행 제안

A는 다섯 항목이고 각각 선택지가 정리되어 있으므로 **20분 내에 결론이 가능하다.** A-4와 A-5는 논의가 아니라 확인 사항이다.

B는 여덟 항목이지만 대부분 이견이 없을 것으로 보인다. **B-1(팀장 이탈)과 B-8(인증 방식)만 짚어도 충분하다.**

C와 D는 다음 회의로 넘겨도 무방하다.
